customCommands:
  - key: '<c-g>'
    context: 'global'
    description: 'Generate AngularJS commit message and copy it'
    command: 'bash -lc "$HOME/.dotfiles/bin/lazygit-ai-commit"'
    output: terminal
