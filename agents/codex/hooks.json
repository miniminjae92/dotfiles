{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-os-core-check\" ]; then \"$HOME/.local/bin/agent-os-core-check\" codex; fi",
            "timeout": 5,
            "statusMessage": "Checking Agent OS core drift"
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-notify\" ]; then \"$HOME/.local/bin/agent-notify\" codex-hook; else \"$HOME/projects/agent-notify/bin/agent-notify\" codex-hook; fi",
            "timeout": 5,
            "statusMessage": "Sending approval notification"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/.local/bin/agent-os-capture-event\"",
            "timeout": 5,
            "statusMessage": "Recording Agent OS event"
          },
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-notify\" ]; then \"$HOME/.local/bin/agent-notify\" codex-hook; else \"$HOME/projects/agent-notify/bin/agent-notify\" codex-hook; fi",
            "timeout": 5,
            "statusMessage": "Sending completion notification"
          }
        ]
      }
    ]
  }
}
