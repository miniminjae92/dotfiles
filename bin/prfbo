#!/bin/sh
root="${PRFB_OUT:-$HOME/.obsidian/mimir/3. Resource/GitHub/PR Feedback}"
selected=$(
  find "$root" -name feedback.md -type f 2>/dev/null |
    fzf --preview 'bat -n --color=always --line-range :200 {}'
)

if [ -n "$selected" ]; then
  nvim "$selected"
fi
