{
  "statusLine": {
    "type": "command",
    "command": "if [ -x \"$HOME/.local/bin/claude-statusline\" ]; then \"$HOME/.local/bin/claude-statusline\"; else \"$HOME/.dotfiles/bin/claude-statusline\"; fi"
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-os-capture-event\" ]; then \"$HOME/.local/bin/agent-os-capture-event\"; fi",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-notify\" ]; then \"$HOME/.local/bin/agent-notify\" claude-hook; else \"$HOME/.dotfiles/bin/agent-notify\" claude-hook; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-notify\" ]; then \"$HOME/.local/bin/agent-notify\" claude-hook; else \"$HOME/.dotfiles/bin/agent-notify\" claude-hook; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/agent-notify\" ]; then \"$HOME/.local/bin/agent-notify\" claude-hook; else \"$HOME/.dotfiles/bin/agent-notify\" claude-hook; fi",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
